function MyInfo({name, email, phone}) {
  return (
    <div>
      <h1>My Personal Information</h1>
      <h2>{name}</h2>
      <h2>{email}</h2>
      <h2>{phone}</h2>
    </div>
  );
}

export default MyInfo;